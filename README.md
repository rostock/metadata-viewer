# metadata-viewer
This VC Map plugin extends the Content Tree with an info button for each layer. When activated, it retrieves the corresponding metadata record from the RecordAPI, parses the data, and presents it in a clearly structured, human-readable window component, providing users with detailed insights into each map layer.
